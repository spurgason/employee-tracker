class Department {
    constructor(id, name)

    getDepartments() {
        const sql = `SELECT department.*
                    department.name AS department`;
    };
}

module.exports = Department;
